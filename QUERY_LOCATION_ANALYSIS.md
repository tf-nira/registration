# SQL Query Location Analysis

## Query Details

The SQL query in question:
```sql
select 
    registrati0_.workflow_instance_id as workflow1_16_,
    registrati0_.applicant_type as applican2_16_,
    registrati0_.cr_dtimes as cr_dtime3_16_,
    registrati0_.cr_by as cr_by4_16_,
    registrati0_.default_resume_action as default_5_16_,
    registrati0_.del_dtimes as del_dtim6_16_,
    registrati0_.is_active as is_activ7_16_,
    registrati0_.is_anonymous_profile_added as is_anony8_16_,
    registrati0_.is_deleted as is_delet9_16_,
    registrati0_.iteration as iterati10_16_,
    registrati0_.lang_code as lang_co11_16_,
    registrati0_.last_success_stage_name as last_su12_16_,
    registrati0_.latest_trn_id as latest_13_16_,
    registrati0_.latest_trn_status_code as latest_14_16_,
    registrati0_.latest_trn_dtimes as latest_15_16_,
    registrati0_.latest_trn_type_code as latest_16_16_,
    registrati0_.needs_notification as needs_n17_16_,
    registrati0_.notification_sent as notific18_16_,
    registrati0_.pkt_cr_dtimes as pkt_cr_19_16_,
    registrati0_.pause_rule_ids as pause_r20_16_,
    registrati0_.ref_reg_id as ref_reg21_16_,
    registrati0_.reg_id as reg_id22_16_,
    registrati0_.reg_process_retry_count as reg_pro23_16_,
    registrati0_.reg_stage_name as reg_sta24_16_,
    registrati0_.process as process25_16_,
    registrati0_.resume_timestamp as resume_26_16_,
    registrati0_.trn_retry_count as trn_ret27_16_,
    registrati0_.source as source28_16_,
    registrati0_.status_code as status_29_16_,
    registrati0_.status_comment as status_30_16_,
    registrati0_.upd_dtimes as upd_dti31_16_,
    registrati0_.upd_by as upd_by32_16_ 
from 
    regprc.registration registrati0_ 
where 
    registrati0_.workflow_instance_id=$1 
    and registrati0_.is_deleted=false 
    and registrati0_.is_active=true
```

## Query Origin

This SQL query is generated by Hibernate/JPA when executing the following JPQL query:

### 1. Repository Layer - RegistrationRepositary.java

**File:** `registration-processor/registration-processor-registration-status-service-impl/src/main/java/io/mosip/registration/processor/status/repositary/RegistrationRepositary.java`

**Line:** 56-57

**Method:**
```java
@Query("SELECT registration FROM RegistrationStatusEntity registration WHERE registration.id.workflowInstanceId = :workflowInstanceId AND registration.isDeleted =false AND registration.isActive=true")
public List<RegistrationStatusEntity> findByWorkflowInstanceId(@Param("workflowInstanceId") String workflowInstanceId);
```

**Entity:** `RegistrationStatusEntity` (maps to `regprc.registration` table)

### 2. Alternative Query Path - SyncRegistrationRepository.java

**File:** `registration-processor/registration-processor-registration-status-service-impl/src/main/java/io/mosip/registration/processor/status/repositary/SyncRegistrationRepository.java`

**Line:** 43-44

**Method:**
```java
@Query("SELECT registrationList FROM SyncRegistrationEntity registrationList WHERE registrationList.workflowInstanceId = :workflowInstanceId AND registrationList.isDeleted =false ")
public List<SyncRegistrationEntity> findByworkflowInstanceId(@Param("workflowInstanceId") String workflowInstanceId);
```

**Note:** This is a similar query but uses `SyncRegistrationEntity` which also maps to the same table.

**Naming Inconsistency:** The repository method is named `findByworkflowInstanceId` (lowercase 'w') while the DAO and service methods use `findByWorkflowInstanceId` (uppercase 'W'). Both spellings are actual method names in the codebase.

## Data Access Layer (DAO)

### RegistrationStatusDao.java

**File:** `registration-processor/registration-processor-registration-status-service-impl/src/main/java/io/mosip/registration/processor/status/dao/RegistrationStatusDao.java`

**Lines:** 119, 138

The DAO layer calls the repository method:
```java
registrationStatusEntityList = registrationStatusRepositary.findByWorkflowInstanceId(workflowInstanceId);
```

### SyncRegistrationDao.java

**File:** `registration-processor/registration-processor-registration-status-service-impl/src/main/java/io/mosip/registration/processor/status/dao/SyncRegistrationDao.java`

**Lines:** 106-110

```java
public SyncRegistrationEntity findByWorkflowInstanceId(String workflowInstanceId) {
    List<SyncRegistrationEntity> syncRegistrationEntityList = syncRegistrationRepository.findByworkflowInstanceId(workflowInstanceId);
    return !CollectionUtils.isEmpty(syncRegistrationEntityList) ? syncRegistrationEntityList.iterator().next() : null;
}
```

## Service Layer

### SyncRegistrationService.java (Interface)

**File:** `registration-processor/registration-processor-registration-status-service-impl/src/main/java/io/mosip/registration/processor/status/service/SyncRegistrationService.java`

**Line:** 56

```java
public SyncRegistrationEntity findByWorkflowInstanceId(String workflowInstanceId);
```

### SyncRegistrationServiceImpl.java (Implementation)

**File:** `registration-processor/registration-processor-registration-status-service-impl/src/main/java/io/mosip/registration/processor/status/service/impl/SyncRegistrationServiceImpl.java`

**Lines:** 616-618

```java
public SyncRegistrationEntity findByWorkflowInstanceId(String workflowInstanceId) {
    return syncRegistrationDao.findByWorkflowInstanceId(workflowInstanceId);
}
```

## Places Where This Query is Executed

### 1. BiometricAuthenticationStage.java

**File:** `registration-processor/core-processor/registration-processor-biometric-authentication-stage/src/main/java/io/mosip/registration/processor/biometric/authentication/stage/BiometricAuthenticationStage.java`

**Line:** 172

**Context:** Biometric authentication stage retrieves registration entity by workflow instance ID.

```java
SyncRegistrationEntity regEntity = syncRegistrationservice
    .findByWorkflowInstanceId(object.getWorkflowInstanceId());
```

### 2. MessageSenderStage.java

**File:** `registration-processor/post-processor/registration-processor-message-sender-stage/src/main/java/io/mosip/registration/processor/message/sender/stage/MessageSenderStage.java`

**Line:** 217

**Context:** Message sender stage retrieves registration entity to send messages.

```java
SyncRegistrationEntity regEntity = syncRegistrationservice.findByWorkflowInstanceId(object.getWorkflowInstanceId());
```

### 3. MVSServiceImpl.java

**File:** `registration-processor/core-processor/registration-processor-mvs-stage/src/main/java/io/mosip/registration/processor/mvs/service/impl/MVSServiceImpl.java`

**Line:** 237

**Context:** Manual Verification Service retrieves registration entity.

```java
SyncRegistrationEntity regEntity = syncRegistrationService.findByWorkflowInstanceId(messageDTO.getWorkflowInstanceId());
```

### 4. PacketValidateProcessor.java

**File:** `registration-processor/pre-processor/registration-processor-packet-validator-stage/src/main/java/io/mosip/registration/processor/stages/packet/validator/PacketValidateProcessor.java`

**Line:** 491

**Context:** Packet validation stage retrieves registration entity.

```java
SyncRegistrationEntity regEntity = syncRegistrationservice
    .findByWorkflowInstanceId(object.getWorkflowInstanceId());
```

### 5. SupervisorApprovalStatusTagGenerator.java

**File:** `registration-processor/pre-processor/registration-processor-packet-classifier-stage/src/main/java/io/mosip/registration/processor/stages/packetclassifier/tagging/impl/SupervisorApprovalStatusTagGenerator.java`

**Line:** 49

**Context:** Tag generator retrieves registration entity for approval status.

```java
SyncRegistrationEntity regEntity = syncRegistrationService.findByWorkflowInstanceId(workflowInstanceId);
```

### 6. SecurezoneNotificationStage.java

**File:** `registration-processor/pre-processor/registration-processor-securezone-notification-stage/src/main/java/io/mosip/registration/processor/securezone/notification/stage/SecurezoneNotificationStage.java`

**Line:** 325

**Context:** Secure zone notification stage retrieves registration entity.

```java
SyncRegistrationEntity regEntity = syncRegistrationService
    .findByWorkflowInstanceId(messageDTO.getWorkflowInstanceId());
```

### 7. PacketUploaderServiceImpl.java

**File:** `registration-processor/pre-processor/registration-processor-packet-uploader-stage/src/main/java/io/mosip/registration/processor/packet/uploader/service/impl/PacketUploaderServiceImpl.java`

**Line:** 202

**Context:** Packet uploader service retrieves registration entity.

```java
regEntity = syncRegistrationService.findByWorkflowInstanceId(messageDTO.getWorkflowInstanceId());
```

## Test Files

The following test files also use this method (through mocks):

1. `MessageSenderStageTest.java` (line 226)
2. `BiometricAuthenticationStageTest.java` (line 309)
3. `VerificationServiceTest.java` (line 340)
4. `RegistrationStatusDaoTest.java` (line 53)
5. `SyncRegistrationDaoTest.java` (lines 151-152)
6. `PacketValidateProcessorTest.java` (lines 158, 327)
7. `SupervisorApprovalStatusTagGeneratorTest.java` (lines 53, 62)
8. `PacketUploaderServiceTest.java` (lines 173, 237)
9. `SecurezoneNotificationStageTest.java` (lines 438, 473, 491, 513, 532)

## Summary

The SQL query fires when any of the following stages or services call the `findByWorkflowInstanceId` method:

1. **Pre-processing stages:**
   - Packet Validator Stage
   - Packet Classifier Stage (Supervisor Approval)
   - Securezone Notification Stage
   - Packet Uploader Stage

2. **Core processing stages:**
   - Biometric Authentication Stage
   - Manual Verification Service (MVS)

3. **Post-processing stages:**
   - Message Sender Stage

All these stages retrieve the registration entity using the workflow instance ID to process registration packets through the workflow pipeline.
